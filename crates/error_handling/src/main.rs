mod unrecoverable_errors;
mod result_enum;
mod match_result_to_recover;
mod propagating_errors;
mod challenge_handle_errors;
//mod collections;

fn main() {
    //unrecoverable_errors::exercise();
    //result_enum::exercise();
    //match_result_to_recover::exercise();
    challenge_handle_errors::handle_lower_higher();
}
